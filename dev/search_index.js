var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-reference","page":"API Reference","text":"","title":"API reference"},{"category":"module","location":"api/#DocumenterSlate","page":"API Reference","text":"DocumenterSlate\n\nPublishes KaimonSlate.jl reactive notebooks as native Documenter.jl pages in CI.\n\nQuick start\n\nusing Documenter, DocumenterSlate\n\nresult = build_slates(\n    SlateBuildOptions(; source = joinpath(@__DIR__, \"..\", \"notebooks\"),\n                       output = joinpath(@__DIR__, \"src\", \"notebooks\")),\n)\n\nmakedocs(; sitename = \"MyPkg.jl\",\n         pages = [\"Home\" => \"index.md\", \"Notebooks\" => result.pages])\n\nbuild_slates is the single public entry point (spec.md §7/§14): it discovers notebooks (discover_notebooks), executes each headlessly via TextualReplayExporter (built on KaimonSlate's own exported standalone! primitive, not the interactive hub — see that type's docstring for why), caches results by a composite fingerprint of every input that affects them (ADR-004), optionally in parallel across separate OS processes (options.nworkers, REQ-EXE-07), converts cells to Documenter Markdown (cell_to_markdown), extracts figures (extract_assets!), and orders everything into .pages (build_pages) for makedocs.\n\nWhere generated files land\n\nbuild_slates writes one .md page and an assets/<notebook-name>/ subdirectory per notebook into options.output. Always git-ignore that directory (REQ-DOC-02): it is regenerated on every docs/make.jl run, never source. Spec.md §6 documents the docs/src/notebooks/-style convention this package's own future example follows.\n\nNotebook format\n\nBuilt against the real KaimonSlate .jl notebook format (#%% md id=... / #%% code id=... cells, @bind, role tags like title/hidecode, an embedded Slate.env dependency footer) as confirmed by reading a local KaimonSlate.jl v1.0.0 checkout during development — not from spec.md's original, partially-guessed examples. See upstream-bugs.md (repository root, not shipped in the package) for what was confirmed against upstream source versus what is still open with the KaimonSlate maintainer.\n\nPublic API (M1–M4)\n\nSlateBuildOptions, SlateOutputOptions, build_slates, discover_notebooks, resolve_notebook_project, NotebookProjectResolution, resolve_notebook_meta, NotebookMeta, AbstractSlateExporter, TextualReplayExporter, execute_notebook, ExecutedNotebook, CellResult, SlateExecutionError, SlateExecutionTimeoutError, cell_to_markdown, extract_assets!, build_pages, SlateBuildResult, verify_slate_bundle, SlateBundleVerification, collect_build_statuses, write_github_step_summary.\n\nThe composite cache (options.cache_dir, options.execution) and parallel execution (options.nworkers) internals (src/cache.jl) are not exported — they're consumed through SlateBuildOptions/build_slates, not called directly.\n\nKnown limitation (see build_slates's docstring for the full write-up): a cache entry's fingerprint validates only its inputs (notebook bytes, environment, tool versions, render options), never the paired .md body itself — execution = :never's trust in options.cache_dir has no cryptographic binding to who actually produced an entry. This doesn't weaken REQ-SEC-03 (no code execution in a privileged job), but it does mean T6 (third-party content published under the repo's identity) isn't mitigated yet. Tracked for M3/M3b (REQ-TRUST-02/03), not fixed here.\n\nNot yet implemented: the SlatePlugin <: Documenter.Plugin / @slate expander (N1) and the :embed/:iframe output formats (M4) — see spec.md §14 for the full milestone plan. CI security hardening and the two-job workflow (M3) are implemented (.github/workflows/docs.yml, docs/src/security.md); the cache-trust known limitation above is what's still open from that milestone, not the workflow itself. build_slates renders cache misses in dedicated Julia processes with a resolved notebook project activated and a deny-by-default environment. Footer-only Slate.env projects are resolved and their manifests are carried in the build cache. The lower-level execute_notebook primitive remains in-process for direct callers.\n\n\n\n\n\n","title":"DocumenterSlate"},{"category":"function","location":"api/#DocumenterSlate.build_slates","page":"API Reference","text":"build_slates(options::SlateBuildOptions,\n             output_options::SlateOutputOptions = SlateOutputOptions()) -> SlateBuildResult\n\nDiscover, execute, and render every notebook under options.source into Documenter-native Markdown pages under options.output (spec §7 — the N0 tier's orchestration entry point, tying together REQ-EXE-01/02/03/04/06/09/10, REQ-REN-01/02/03/05/06/09/10, REQ-CACHE-01/02, REQ-INT-02).\n\nPipeline\n\ndiscover_notebooks(options).\nFor each discovered path, resolve_notebook_meta(path, options.slate_toml) (front-matter, docs/slate.toml overrides).\nFor every non-skip notebook, refuse any region=<name> tag unless options.allow_remote == true (REQ-EXE-09), then _build_one_notebook: resolve the notebook's pinned environment (resolve_notebook_project) and composite cache fingerprint (ADR-004), then:\nexecution = :auto (default): a cache hit copies straight from options.cache_dir (REQ-CACHE-01); a miss executes and refreshes the cache.\nexecution = :always: always executes (via options.exporter, defaulting to a fresh TextualReplayExporter), still refreshing the cache for a later build.\nexecution = :never (REQ-EXE-10, hermetic build): a cache hit copies from options.cache_dir; a miss raises ArgumentError rather than executing anything — this is the mode the privileged deploy job in spec §9's reference workflow runs under.\nA hit/miss/execute always passes options.fail_on_error and the notebook's resolved binds into execute_notebook, extracts assets via extract_assets! when output_options.assets == :files (:base64, REQ-REN-04, is Could and not implemented), and renders cells via cell_to_markdown (anchor-prefixed by the notebook's slug, REQ-REN-09).\nbuild_pages over every discovered (path, meta) pair (including skip entries, which it filters) to produce the ordered .pages.\n\nKnown limitation: execution = :never's cache trust has no producer binding\n\n(M2 security review finding, recorded here since it's the first milestone where :never is actually functional.) The composite fingerprint (ADR-004) is computed only from inputs — notebook bytes, resolved environment, tool versions, render options — all of which spec.md itself requires publishing (REQ-DIST-01/02) or are checked into the repo. It never hashes the paired .md body. A party who can place a <slug>.md + <slug>.cache.toml pair with a matching self-reported fingerprint into whatever directory later becomes options.cache_dir for a privileged execution = :never build can have arbitrary content published verbatim — _write_output_from_cache copies it through with none of cell_to_markdown's REQ-SEC-05 escaping, since that only runs on the execute-then-render path. REQ-SEC-03's \"no code execution in the privileged job\" guarantee still holds (a forged entry is a cache hit, never triggers execute_notebook), but T6 (\"third-party content published under repo identity\") is not mitigated by anything in src/cache.jl/src/build.jl today. Closing this needs binding a cache entry to who produced it (REQ-TRUST-02's PROVENANCE.toml/CI run ID, or REQ-TRUST-03's build attestation) — a stronger hash of the same public inputs cannot fix it. Tracked for M3 (before the two-job workflow is considered to hold end-to-end) / M3b, not fixed here.\n\nHistorical note: spec.md §9's illustrative example workflow had this bug\n\nspec.md §9's illustrative reference workflow (non-normative prose, not shipped code) only restored docs/src/notebooks via actions/download-artifact — it never restored docs/slate_cache. As written, that example's execution = :never call would have hit this function's ArgumentError on every run, since options.cache_dir would always be empty. Fixed in the actual shipped workflow: .github/workflows/docs.yml uploads docs/slate_cache as an artifact in render-notebooks and downloads that same artifact in build-and-deploy before docs/make.jl runs — execution = :never there has a genuinely populated cache to read. This note stays only as the historical record of the bug spec.md's own prose sketch had; it does not describe a gap in this repository's real workflow.\n\nEffective show_code\n\nA cell's source is shown only when both output_options.show_code (the site-wide default) and the notebook's own resolved meta.show_code (REQ-REN-05, from docs/slate.toml, defaulting to true) are true — either one being false hides it. meta.show_code's per-notebook resolution (M1.7) doesn't currently distinguish \"explicitly set to true in docs/slate.toml\" from \"left at its own default\", so this AND-combination is the simplest sound reading and is called out here as a judgment call rather than a directly cited requirement. This also feeds the cache key (ADR-004's \"hash des options de rendu\", widened — see cache.jl's _render_option_hash), so a show_code change correctly invalidates a stale cache entry rather than silently reusing mismatched output.\n\nnworkers > 1 (REQ-EXE-07)\n\nNotebooks are built across min(options.nworkers, <number to build>) separate OS processes via _build_parallel! — not threads; see that function's docstring for why (src/execute.jl's stdout-capture lock makes threaded fan-out either corrupt output or fully serialize, not actually parallelize). Falls back to the plain sequential loop when nworkers <= 1 or there is at most one notebook to build (not worth process-spawn overhead). Each dispatched notebook still does its own cache lookup independently (on whichever worker it lands on) — a hit is cheap disk I/O, not worth special-casing out of the worker pool. pmap's result order matches input order regardless of completion order, so worker scheduling never disturbs build_pages's ordering.\n\nScope not yet implemented (raises ArgumentError rather than silently mis-behaving)\n\noutput_options.format != :documenter (:embed/:iframe, REQ-REN-11) — N2 tier, M4.\n\nDownload links and per-artifact provenance (M3b) are generated for every notebook. The output_options.provenance/.downloads selectors and a separate provenance footer (REQ-REN-07) remain inert until the configurable expander work in M4. REQ-CACHE-05's check_slates() non-determinism detector is explicitly deferred past M2 (it needs a second execution to diff against the cache, which is exactly the cost :auto/:never exist to avoid).\n\nCache misses are rendered in a dedicated child process. A manifest-backed notebook project is activated before execution, the parent environment is not inherited, and timeout hard-kills the worker process. Footer-only environments are resolved and instantiated in another isolated process; their Project and Manifest are cached for the non-executing deployment job.\n\n\n\n\n\n","title":"DocumenterSlate.build_slates"},{"category":"type","location":"api/#DocumenterSlate.SlateBuildOptions","page":"API Reference","text":"SlateBuildOptions(; source, output, kwargs...)\n\nOptions controlling notebook discovery, execution and caching for build_slates (spec §7, REQ-EXE-, REQ-CACHE-).\n\nsource and output are required keyword arguments: there is no sensible default source or destination directory for a given documentation build, so omitting either is an error.\n\nKeywords\n\nsource::AbstractString: directory containing the KaimonSlate notebooks (required).\noutput::AbstractString: directory the generated Markdown pages are written to (required).\ninclude::Vector{String} = [\"*.jl\"]: glob patterns selecting notebooks under source.\nexclude::Vector{String} = [\"_*.jl\"]: glob patterns excluding notebooks under source.\nexporter = nothing: execution backend (TextualReplayExporter() by default). During build_slates, textual replay runs in a dedicated child process.\nexecution::Symbol = :auto: one of :auto, :always, :never.\ncache_dir::AbstractString = \"slate_cache\": directory holding the build cache.\nnworkers::Integer = 1: number of notebooks executed in parallel.\nfail_on_error::Bool = true: whether a cell exception aborts the build (REQ-EXE-04/05).\nallow_remote::Bool = false: whether notebooks containing region=<name> cell tags may be built. The default refusal requires an explicit opt-in (REQ-EXE-09).\nworker_environment::Dict{String,String} = Dict(): explicit environment variables passed to the isolated notebook process. The parent process environment is not inherited.\nslate_toml::Union{Nothing,AbstractString} = nothing: path to a docs/slate.toml-shaped declarative config (spec §10) providing per-notebook order/skip/show_code/binds overrides to resolve_notebook_meta. Added in M1.13 (not part of spec §7's original sketch, which didn't say how build_slates would locate it) — no auto-discovery, must be given explicitly if wanted.\n\n\n\n\n\n","title":"DocumenterSlate.SlateBuildOptions"},{"category":"type","location":"api/#DocumenterSlate.SlateOutputOptions","page":"API Reference","text":"SlateOutputOptions(; kwargs...)\n\nOptions controlling how rendered notebooks are formatted (spec §7, REQ-REN-*, ADR-005).\n\nKeywords\n\nformat::Symbol = :documenter: one of :documenter, :embed, :iframe.\ninteractivity::Symbol = :frozen: one of :frozen, :client (ADR-005 default is :frozen).\nshow_code::Bool = true: whether cell source is rendered alongside outputs.\nassets::Symbol = :files: one of :files, :base64.\nprovenance::Bool = true: whether a provenance footer is added (REQ-REN-07).\ndownloads::Vector{Symbol} = [:html, :pdf, :source]: download links shown on the page.\ncanonical::Union{Nothing,AbstractString} = nothing: the published site's base URL (the same value Documenter's HTML(canonical = ...) takes). When set, each notebook's download workflow shows the absolute archive URL a reader can pass to fetch_slate_bundle; when unset, the workflow falls back to the archive's site-relative path. It must be an https:// URL: the workflow's first step fetches from it, and an unauthenticated transport is not offered.\n\n\n\n\n\n","title":"DocumenterSlate.SlateOutputOptions"},{"category":"function","location":"api/#DocumenterSlate.discover_notebooks","page":"API Reference","text":"discover_notebooks(source::AbstractString; include=[\"*.jl\"], exclude=[\"_*.jl\"]) -> Vector{String}\ndiscover_notebooks(options::SlateBuildOptions) -> Vector{String}\n\nDiscover KaimonSlate notebooks under source (or options.source) matching any include glob pattern and none of the exclude glob patterns (spec §7, REQ-EXE-01).\n\nReturns absolute, normalized paths sorted alphabetically so that build order is reproducible across runs and machines.\n\nsource (or options.source) must exist, or an ArgumentError is thrown rather than silently returning an empty result.\n\nEvery discovered path is resolved (realpath) and checked to still live inside the resolved source directory before being returned; a notebook whose resolved path escapes source — for example via a symlink planted inside source that points outside it — causes an ArgumentError rather than being included (REQ-SEC-06, directory traversal).\n\n\n\n\n\n","title":"DocumenterSlate.discover_notebooks"},{"category":"function","location":"api/#DocumenterSlate.resolve_notebook_project","page":"API Reference","text":"resolve_notebook_project(notebook_path::AbstractString) -> NotebookProjectResolution\n\nResolve the pinned dependency environment for the KaimonSlate notebook at notebook_path (spec §17 addendum, ADR-004; upstream-bugs.md §8 Decision A).\n\nResolution order:\n\nIf a Project.toml exists in the notebook's own directory, that directory is treated as the real pinned project (kind = :external). Only the notebook's immediate directory is checked — ancestor directories are not walked, to avoid accidentally picking up an unrelated ancestor Project.toml (for example a surrounding package repository's own root Project.toml).\nOtherwise, the notebook's embedded Slate.env footer is parsed via KaimonSlate.parse_report and used as a weaker fallback (kind = :footer). A @warn is logged documenting that this is an unresolved-dependency-only fallback with no Pkg.resolve performed.\n\n\n\n\n\n","title":"DocumenterSlate.resolve_notebook_project"},{"category":"type","location":"api/#DocumenterSlate.NotebookProjectResolution","page":"API Reference","text":"NotebookProjectResolution\n\nResult of resolving the pinned dependency environment for a single KaimonSlate notebook (spec §17 addendum, ADR-004; upstream-bugs.md §8 Decision A).\n\nReal KaimonSlate notebooks carry their own dependencies as an embedded Slate.env footer (name/version/UUID triples), not a resolved Manifest.toml. The resolution order is:\n\n:external — an external Project.toml sits beside the notebook. project_dir holds its directory and manifest_path holds the path to a sibling Manifest.toml, if one exists (nothing otherwise). env_footer is nothing.\n:footer — no external Project.toml was found, so the literal Slate.env footer text embedded in the notebook is used as a weaker, unresolved-dependency cache-key/provenance component instead. env_footer holds the parsed footer entries (each a Dict with \"name\", \"version\", \"uuid\"); project_dir and manifest_path are nothing.\n\nOn a cache miss, isolated builds materialize a temporary project from these entries, resolve and instantiate it without inheriting the parent environment, and cache the resulting manifest for the non-executing documentation job.\n\nFields\n\nkind::Symbol: :external or :footer.\nnotebook_path::String: resolved (realpath) path of the notebook this resolution is for.\nproject_dir::Union{Nothing,String}: resolved directory containing the external Project.toml, or nothing for the :footer case.\nmanifest_path::Union{Nothing,String}: resolved path of a sibling Manifest.toml next to the external Project.toml, or nothing when absent or not applicable.\nenv_footer::Union{Nothing,Vector{Dict{String,Any}}}: parsed Slate.env footer entries (KaimonSlate.parse_report(...).meta[\"env\"]), or nothing for the :external case.\n\n\n\n\n\n","title":"DocumenterSlate.NotebookProjectResolution"},{"category":"function","location":"api/#DocumenterSlate.resolve_notebook_meta","page":"API Reference","text":"resolve_notebook_meta(notebook_path::AbstractString, slate_toml_path=nothing) -> NotebookMeta\n\nResolve front-matter for the KaimonSlate notebook at notebook_path (spec §7/§10, §17 addendum; upstream-bugs.md §8 Decision B).\n\ntitle is resolved from the notebook itself (role-tagged cell → first H1 → filename, see NotebookMeta); order, skip, show_code and binds fall back to nothing/false/true/Dict() unless overridden.\n\nIf slate_toml_path is given, it is parsed and searched for a [[notebook]] table entry whose path field's filename matches notebook_path's filename; any of title, order, skip, show_code, binds present in that entry override the notebook's own resolution.\n\n\n\n\n\n","title":"DocumenterSlate.resolve_notebook_meta"},{"category":"type","location":"api/#DocumenterSlate.NotebookMeta","page":"API Reference","text":"NotebookMeta\n\nResult of resolving front-matter for a single KaimonSlate notebook (spec §7/§10, §17 addendum; upstream-bugs.md §8 Decision B).\n\nReal KaimonSlate notebooks carry title/abstract/bibliography as role-tagged cells (Cell.flags), not a [documenter] TOML block in cell 1. title is resolved via the same fallback chain KaimonSlate's own PDF/Typst export uses (export_typst.jl's report_frontmatter/_parse_title_cell, not itself exported so reimplemented here per the same \"avoid unexported internals\" convention as resolve_notebook_project):\n\na :title-tagged cell's first Markdown H1 line, else\nthe first Markdown H1 in any cell, in cell order, else\nthe notebook's filename without its extension.\n\nFields DocumenterSlate needs that upstream has no opinion on (order, skip, show_code, binds) come from a matching [[notebook]] table in docs/slate.toml (spec §10), matched by notebook filename. When no slate.toml is given, or no entry matches this notebook, they fall back to order = nothing, skip = false, show_code = true (mirroring SlateOutputOptions's own show_code default) and binds = Dict().\n\nFields\n\ntitle::String: resolved notebook title.\norder::Union{Nothing,Real}: page ordering hint, or nothing when unset.\nskip::Bool: whether this notebook should be excluded from the build.\nshow_code::Bool: whether cell source is rendered alongside outputs.\nbinds::Dict{String,Any}: @bind values to apply before execution (REQ-EXE-08).\n\n\n\n\n\n","title":"DocumenterSlate.NotebookMeta"},{"category":"type","location":"api/#DocumenterSlate.AbstractSlateExporter","page":"API Reference","text":"AbstractSlateExporter\n\nAbstract supertype for headless notebook execution backends (spec §5 ADR-003, revised per upstream-bugs.md §7: three tiers instead of two).\n\nConcrete subtypes so far:\n\nTextualReplayExporter: M1 default, built on KaimonSlate.standalone!, zero dependency on the hub.\n\nDeferred / not yet implemented:\n\nHubExporter (M2+): self-contained PDF/HTML export via the local slate hub, supports interactivity.\nInProcessExporter: speculative, unconfirmed upstream — do not assume it exists.\n\nThis type only defines the exporter hierarchy (M1.8); execution behavior (KaimonSlate.standalone! invocation, timeout enforcement per REQ-EXE-06) is added separately (M1.9).\n\n\n\n\n\n","title":"DocumenterSlate.AbstractSlateExporter"},{"category":"type","location":"api/#DocumenterSlate.TextualReplayExporter","page":"API Reference","text":"TextualReplayExporter(; timeout::Int = 900)\n\nHeadless execution backend built on KaimonSlate.standalone! (spec §5 ADR-003, revised; see upstream-bugs.md §7). This is the M1 default exporter: it has zero dependency on the slate hub and evaluates a notebook's cells top-to-bottom in a fresh module, Pluto-style.\n\nKeywords\n\ntimeout::Int = 900: maximum wall-clock time, in seconds, allowed for a single notebook's execution (REQ-EXE-06) before execute_notebook raises SlateExecutionTimeoutError. Default of 900s (15 minutes) matches spec §5's original HubExporter sketch, applied here to the current default exporter. Must be positive.\nNote: TextualReplayExporter executes in-process, so this is detection, not a hard kill — a non-yielding, CPU-bound cell keeps running in the background after the error is raised to the caller. See execute_notebook's docstring for the exact boundary and why a true kill needs an out-of-process backend (HubExporter, M2+).\n\nExecution behavior (actually invoking KaimonSlate.standalone! and enforcing timeout) is implemented separately; this type only carries the configuration.\n\n\n\n\n\n","title":"DocumenterSlate.TextualReplayExporter"},{"category":"function","location":"api/#DocumenterSlate.execute_notebook","page":"API Reference","text":"execute_notebook(exporter::TextualReplayExporter, notebook_path::AbstractString;\n                  fail_on_error::Bool = true, binds::Dict = Dict()) -> ExecutedNotebook\n\nExecute a KaimonSlate notebook headlessly, cell by cell, in source order (REQ-EXE-02/03, spec §17 addendum / upstream-bugs.md §7, ADR-003 revision). This is the TextualReplayExporter half of AbstractSlateExporter (spec §5 ADR-003): built on the confirmed, exported upstream primitive KaimonSlate.standalone!, with zero dependency on the slate hub (REQ-EXE-09, verified by construction — this file never references HTTP/Sockets/hub or remote machinery).\n\nExecution model\n\nnotebook_path is parsed via KaimonSlate.parse_report.\nA fresh, anonymous Module is created for this call — never reused, never shared with the calling process's globals.\nKaimonSlate.standalone!(mod; dir = dirname(notebook_path)) injects the notebook-namespace contract (@bind, widgets, …) into it, per the confirmed upstream contract (upstream-bugs.md §7): @bind x W(…) resolves to W's default with no browser/hub involved.\nCells are evaluated in source order — no reactive-DAG scheduling (ADR-003's revision: execute_notebook never calls into KaimonSlate's dependency/deps machinery).\n:md cells are recorded as-is; no evaluation. ({{ expr }} markdown interpolation is out of scope here — see CellResult's \"Fields\" note above and the M1.10 rendering task, which is a more natural home for it.)\n:code (and WEB, which evaluates identically upstream) cells are evaluated via include-equivalent semantics (Base.include_string): stdout is captured, the last top-level expression's value is captured, and a thrown exception is caught with its backtrace.\n\nfail_on_error (REQ-EXE-04/05)\n\ntrue (default): the first cell that throws stops execution — later cells are not run — and SlateExecutionError is raised, identifying the notebook path, the cell's id/index, its source, and the first 20 lines of the formatted backtrace.\nfalse: the error is recorded on that cell's CellResult (.error/ .backtrace) and execution continues with the next cell.\n\nbinds (REQ-EXE-08, Should)\n\nA Dict from bound variable name (as a String) to override value. For each :code cell whose source contains @bind <name> Widget(...), if <name> is a key of binds, the override is applied via the notebook namespace's own __slate_set_bind function (the same one the browser calls on a live control change) immediately after that cell runs — i.e. before any later cell that reads the bound variable. This is a source-text match, not an AST/dependency-graph one (see _apply_bind_override!'s comment for why); sufficient for the Should-priority scope of REQ-EXE-08.\n\ntimeout (REQ-EXE-06)\n\nEnforced per notebook (matching the spec's \"l'exécution d'un notebook\" wording), not per cell, using exporter.timeout seconds. The cell loop runs inside a Threads.@spawn'd task; timedwait polls for completion. On timeout, SlateExecutionTimeoutError is raised.\n\nBecause TextualReplayExporter executes in-process (no subprocess/worker to kill, unlike the spec's \"tuer le worker\" wording, which describes the future HubExporter), a timeout can only be detected reliably for cell code that yields control at some point (I/O, sleep, task switches, GC safepoints under normal execution) — the background task is not forcibly terminated, so a genuinely non-yielding, CPU-bound infinite loop keeps running after SlateExecutionTimeoutError is raised to the caller (a real resource leak, not just a cosmetic one). This is a known, explicit limitation of the in-process textual-replay approach, not a bug; a true kill requires an out-of-process backend.\n\nENV/REQ-SEC-02 nuance\n\nThe fresh module does not additionally import or widen access to the calling process's ENV beyond what any Julia code can already see: Base.ENV is a process-global binding, reachable from any Julia code in any module regardless of what execute_notebook does — there is no module-scoped sandboxing of it in stock Julia, and this function does not attempt one. What execute_notebook does guarantee is narrower and real: it never explicitly copies, injects, or otherwise makes ENV \"more reachable\" than it already is (e.g. it does not pass ENV as an argument into the module, define a convenience alias, or widen any whitelist). A notebook cell that calls ENV[\"SECRET\"] directly succeeds regardless — full process-wide ENV containment (REQ-SEC-02's CI-integration half — a whitelist propagated into a genuinely separate worker process) is out of scope for an in-process executor and is deferred to a future out-of-process backend (HubExporter+CI-level environment scrubbing, spec §9's two-job workflow).\n\n\n\n\n\n","title":"DocumenterSlate.execute_notebook"},{"category":"type","location":"api/#DocumenterSlate.ExecutedNotebook","page":"API Reference","text":"ExecutedNotebook\n\nResult of executing a whole notebook via execute_notebook (REQ-EXE-03).\n\nFields\n\nnotebook_path::String: resolved (realpath) path of the executed notebook.\ncells::Vector{CellResult}: per-cell results, in source order. When fail_on_error = true this only reaches the caller for a notebook that executed to completion (an error instead raises SlateExecutionError); when fail_on_error = false, all cells are present, including any recorded with an error.\nmod::Module: the fresh, anonymous module the notebook was evaluated in. Kept for downstream rendering (M1.10) that may need to inspect leftover module state (e.g. a display object a cell only assigned to a variable without it being the last expression). Not needed for REQ-EXE-03/04/05/06 themselves.\n\n\n\n\n\n","title":"DocumenterSlate.ExecutedNotebook"},{"category":"type","location":"api/#DocumenterSlate.CellResult","page":"API Reference","text":"CellResult\n\nCaptured outcome of executing (or, for a markdown cell, simply carrying) a single notebook cell (REQ-EXE-03/04/05).\n\nFields\n\ncell_id::String: the cell's persistent id (KaimonSlate.ReportEngine.Cell.id).\nkind::Symbol: :md or :code. KaimonSlate's WEB cell kind is treated as :code (it evaluates identically — see widgets.jl's own comment on WEB cells).\nsource::String: the cell's source text, exactly as KaimonSlate.parse_report returns it (for :md cells, already unwrapped from the @md\"\"\"…\"\"\" runnable skin — see KaimonSlate.jl/src/engine.jl's _unwrap_md). Carried through for downstream rendering (M1.10) so it doesn't need to re-parse the notebook file.\nflags::Set{Symbol}: the cell's role/UI tags (KaimonSlate.ReportEngine.Cell.flags, e.g. :title, :hidecode) — also carried through for M1.10 rendering.\nstdout_text::String: captured standard output produced while evaluating the cell; always \"\" for a :md cell.\nvalue::Any: the cell's last top-level expression value (mirrors include); nothing for :md cells and for :code cells whose last statement has no value (e.g. a bare using X), and for a cell recorded with an error.\nerror::Union{Nothing,Exception}: the exception raised while evaluating this cell, or nothing. Only ever set when fail_on_error = false (with fail_on_error = true, execute_notebook raises SlateExecutionError instead of returning).\nbacktrace::Union{Nothing,String}: the formatted backtrace of error (full length, not truncated — the 20-line cap of REQ-EXE-04 applies only to the raised SlateExecutionError, not to a recorded per-cell result), or nothing.\n\n\n\n\n\n","title":"DocumenterSlate.CellResult"},{"category":"type","location":"api/#DocumenterSlate.SlateExecutionError","page":"API Reference","text":"SlateExecutionError <: Exception\n\nRaised by execute_notebook when a code cell throws and fail_on_error = true (REQ-EXE-04). Identifies the notebook file, the failing cell's id and 1-based index (over all cells, markdown included), the cell's source code, and the first 20 lines of the formatted backtrace.\n\nCustom exception type (rather than a rethrow of the raw exception) so callers can catch e; e isa SlateExecutionError instead of pattern-matching on arbitrary user code exceptions.\n\nFields\n\nnotebook_path::String\ncell_id::String\ncell_index::Int\ncell_source::String\nbacktrace_lines::Vector{String}: at most the first 20 lines of the formatted backtrace of the underlying exception.\n\n\n\n\n\n","title":"DocumenterSlate.SlateExecutionError"},{"category":"type","location":"api/#DocumenterSlate.SlateExecutionTimeoutError","page":"API Reference","text":"SlateExecutionTimeoutError <: Exception\n\nRaised by execute_notebook when a notebook's total execution time exceeds exporter.timeout seconds (REQ-EXE-06). Distinct from SlateExecutionError so callers can tell a per-cell failure apart from a timeout.\n\nNote: TextualReplayExporter executes in-process, so a timeout cannot forcibly kill a non-yielding (CPU-bound, no I/O/sleep/task-switch) computation — see execute_notebook's docstring for the exact boundary.\n\n\n\n\n\n","title":"DocumenterSlate.SlateExecutionTimeoutError"},{"category":"function","location":"api/#DocumenterSlate.cell_to_markdown","page":"API Reference","text":"cell_to_markdown(cell::CellResult; show_code::Bool = true,\n                  anchor_prefix::AbstractString = \"\",\n                  asset_path::Union{Nothing,AbstractString} = nothing) -> String\n\nConvert one executed cell (REQ-REN-01) into a Documenter-native Markdown fragment.\n\nMarkdown cells (cell.kind == :md)\n\nReturned as-is (KaimonSlate.parse_report already unwraps the @md\"\"\"…\"\"\" runnable skin, so cell.source is already plain Markdown — REQ-REN-02), except each ATX heading is rewritten with Documenter's [title](@id anchor) syntax, prefixed with anchor_prefix (REQ-REN-09: guarantees uniqueness across notebook pages built into the same site, since Documenter's own heading-derived anchors are only guaranteed unique within a single generated page). {{ … }} markdown interpolation is out of scope here, same as M1.9's execution step — see that task's notes; none of this package's fixtures use it.\n\nREQ-SEC-05 scope note (recorded during M3 planning): a markdown cell's source — including any raw HTML it contains — is passed through to Documenter's CommonMark pipeline unmodified, by design. This matches ordinary Documenter .md-source-file semantics and is required for REQ-REN-02 (native headings feed navigation/search). It is not covered by REQ-SEC-05's \"escape content not explicitly marked as raw HTML\" guarantee, which applies to derived/executed content — stdout_text, value, error messages — not author-written prose. A notebook's markdown cells carry the same trust level as any other Documenter source page; review changes to notebooks//docs/notebooks/ at the same rigor as source code (spec §12 T6's own mitigation note makes the same recommendation).\n\nCode cells (cell.kind == :code)\n\nSource is shown as a `julia fence when show_code && !(:hidecode in cell.flags) (REQ-REN-05: the hidecode tag always wins over show_code, since it is the cell author's own explicit choice).\nIf the cell recorded an error (cell.error !== nothing — only possible when the notebook was executed with fail_on_error = false), the output is a Documenter !!! error admonition containing the formatted exception and its backtrace, so the failure is visible on the page rather than silently dropped (REQ-EXE-05).\nOtherwise, if asset_path is given (the relative path extract_assets! wrote the cell's value to — REQ-REN-03), the output is a single ![](asset_path) image link instead of a text rendering of the value. Otherwise, non-empty stdout_text and/or a non-nothing value are rendered as a plain, untagged fenced block — never `jldoctest (REQ-REN-10: notebook output must not be picked up by Documenter's doctest runner). A cell with neither stdout nor a displayable value (e.g. ending in a bare using X or nothing) and no asset_path produces no output block at all.\n\nPreserves literal $…$/$$…$$ math untouched in both branches (REQ-REN-06) — no rewriting is performed, so whatever KaTeX/MathJax3 syntax the notebook author used passes straight through.\n\n\n\n\n\n","title":"DocumenterSlate.cell_to_markdown"},{"category":"function","location":"api/#DocumenterSlate.extract_assets!","page":"API Reference","text":"extract_assets!(executed::ExecutedNotebook, dest_dir::AbstractString) -> Dict{String,String}\n\nWrite every code cell's displayable figure output to dest_dir (REQ-REN-03), creating it if needed. A cell's .value is checked against _ASSET_MIME_EXTENSIONS in order; the first MIME type it is showable as is written to a deterministically-numbered file (fig-01.png, fig-02.svg, …, numbered across the whole notebook in cell order — not per-MIME-type). Cells with an .error, a nothing .value, or a .value that isn't showable as any tracked image MIME type produce no asset and no entry.\n\nReturns a Dict mapping each such cell's cell_id to the bare filename written inside dest_dir (e.g. \"fig-01.png\", not a path). Composing that into the assets/<notebook-name>/fig-01.png-style relative link a rendered page actually embeds (REQ-REN-03) is the caller's job (M1.12 page assembly), since only the caller knows the notebook's name/slug — this function only knows the one dest_dir it was given, kept decoupled and easy to test in isolation.\n\nPass the resulting filename to cell_to_markdown's asset_path keyword (joined with whatever the caller's relative-link convention is) to embed the figure instead of a text rendering of the cell's raw value.\n\n\n\n\n\n","title":"DocumenterSlate.extract_assets!"},{"category":"function","location":"api/#DocumenterSlate.build_pages","page":"API Reference","text":"build_pages(entries) -> SlateBuildResult\n\nentries is an iterable of (notebook_path, meta::NotebookMeta) tuples, typically built by calling resolve_notebook_meta over the output of discover_notebooks.\n\nFilters out every entry with meta.skip == true (REQ-INT-02), then orders the rest (REQ-INT-02: \"ordonnée selon le champ order du front-matter puis alphabétiquement\"): entries with a non-nothing meta.order come first, sorted ascending by that value; entries with no explicit order follow, sorted alphabetically by meta.title. Ties (two entries sharing the same explicit order, or two unordered entries with the same title) are broken alphabetically by title. This \"explicit order pins the front, everything else follows alphabetically\" rule isn't pinned down further by spec.md — it is the most common documentation-generator convention and is called out here as a judgment call rather than a directly cited requirement.\n\nEach page's relative output path is the notebook's basename with its extension replaced by .md (e.g. notebooks/analysis.jl → \"analysis.md\") — bare, not prefixed with a directory, since only the caller (M1.13's build_slates) knows the actual output-directory layout it is writing into.\n\n\n\n\n\n","title":"DocumenterSlate.build_pages"},{"category":"type","location":"api/#DocumenterSlate.SlateBuildResult","page":"API Reference","text":"SlateBuildResult\n\nResult of ordering a set of notebooks into Documenter pages entries (build_pages, REQ-INT-02).\n\nFields\n\npages::Vector{Pair{String,String}}: title => relative_output_path, directly insertable into makedocs(; pages = ...) (spec §7). Directly Vector{Pair{String,String}} rather than nesting under a section name — spec §7's example wraps it itself (\"Notebooks\" => slates.pages), so this type doesn't presume a section title.\noutput_dir::Union{Nothing,String}: absolute directory containing the rendered pages when the result came from build_slates; used by SlatePlugin's @slate expander.\n\n\n\n\n\n","title":"DocumenterSlate.SlateBuildResult"},{"category":"type","location":"api/#DocumenterSlate.SlatePlugin","page":"API Reference","text":"SlatePlugin([slates])\n\nDocumenter plugin state for notebook pages produced by build_slates. Pass an instance to makedocs(; plugins = [SlatePlugin(slates)]). The zero-argument constructor supports Documenter's getplugin fallback and represents a build for which no slate result was supplied.\n\nWith the populated plugin installed, an authored documentation page can embed a rendered notebook as native Documenter content:\n\n```@slate\nnotebook = \"analysis.jl\"\n```\n\nUse the same configuration under an @slate-download block to insert only the verified download and inactive-inspection panel in an authored page.\n\n\n\n\n\n","title":"DocumenterSlate.SlatePlugin"},{"category":"function","location":"api/#DocumenterSlate.verify_slate_bundle","page":"API Reference","text":"verify_slate_bundle(path) -> SlateBundleVerification\n\nVerify a downloaded DocumenterSlate distribution directory or .tar.gz archive without executing notebook or package code. Verification requires an exhaustive SHA256SUMS, checks every listed artifact, and cross-checks the notebook source and optional manifest against PROVENANCE.toml. Archives containing links, nested paths, or non-regular entries are rejected before extraction.\n\nAn ArgumentError is thrown on any integrity or structural failure. Forge attestations are not accepted until DocumenterSlate can authenticate them; provenance metadata alone is not treated as a signature.\n\n\n\n\n\n","title":"DocumenterSlate.verify_slate_bundle"},{"category":"type","location":"api/#DocumenterSlate.SlateBundleVerification","page":"API Reference","text":"SlateBundleVerification\n\nSuccessful result returned by verify_slate_bundle.\n\nartifacts contains the checksum-covered filenames, provenance is the parsed PROVENANCE.toml, and archive records whether verification required safe extraction of a .tar.gz archive.\n\n\n\n\n\n","title":"DocumenterSlate.SlateBundleVerification"},{"category":"function","location":"api/#DocumenterSlate.fetch_slate_bundle","page":"API Reference","text":"fetch_slate_bundle(url; into = mktempdir()) -> SlateBundle\n\nDownload a notebook bundle over https, verify it, and extract it — without executing notebook code, instantiating its environment, or running package build scripts.\n\nurl may also be a local .tar.gz path, for a bundle you already downloaded by other means.\n\nThis is step 1 of the download workflow shown on every notebook page. It fails loudly rather than returning an unverified bundle: the archive's entries are checked for links and nested paths before extraction, every artifact is checked against SHA256SUMS, and the notebook source and optional manifest are cross-checked against PROVENANCE.toml (see verify_slate_bundle). The extracted directory is then verified in place.\n\nWhat this deliberately does not do is launch anything. Verification establishes that the bytes are the ones the publisher hashed; it establishes nothing about origin or intent. Judge bundle.provenance and read bundle.source before running the notebook — displaying the returned SlateBundle prints those remaining steps.\n\nAn ArgumentError is thrown on an unusable URL, a failed download, or any integrity or structural failure.\n\n\n\n\n\n","title":"DocumenterSlate.fetch_slate_bundle"},{"category":"type","location":"api/#DocumenterSlate.SlateBundle","page":"API Reference","text":"SlateBundle\n\nA downloaded, verified, extracted notebook bundle, returned by fetch_slate_bundle.\n\ndirectory is the extracted bundle, archive the local .tar.gz it came from, source the notebook's .jl file, provenance the parsed PROVENANCE.toml, and verification the SlateBundleVerification produced while checking the archive.\n\nHolding a SlateBundle means the bytes were checked, not that the code is safe: nothing in it has been read, instantiated, or executed. Displaying one prints the remaining steps of the download workflow.\n\n\n\n\n\n","title":"DocumenterSlate.SlateBundle"},{"category":"function","location":"api/#DocumenterSlate.collect_build_statuses","page":"API Reference","text":"collect_build_statuses(f) -> Vector{<:NamedTuple}\n\nRuns f() (typically () -> build_slates(options, output_options)) while capturing every per-notebook \"notebook build\" @info record build_slates emits (REQ-CI-02, M2.6), returning them as (slug, status, elapsed_s) NamedTuples in emission order.\n\nbuild_slates's own return type (SlateBuildResult) intentionally doesn't carry this — it's build-order information (.pages), not build-status information, and widening it would touch every existing caller/test of build_slates. This function taps the logging output instead (a real, already-emitted signal, not a parallel bookkeeping path that could drift from what actually happened).\n\nLimitation: sequential builds only (options.nworkers <= 1)\n\nVerified empirically, not assumed: @info calls made inside a _build_parallel! pmap-dispatched worker task are not routed through the calling process's Logging dispatch — Distributed instead forwards each worker's log output as pre-formatted text straight to the master's stdout (visible in a CI job's raw log either way), bypassing Logging.with_logger/handle_message entirely. This function's capture mechanism therefore only sees records from a sequential build. A nworkers > 1 build's per-notebook statuses are still logged normally and visible in CI output, just not collectible here — building a cross-process structured-logging bridge is out of scope for this Could-priority utility (REQ-CI-04).\n\nPair with write_github_step_summary to build a $GITHUB_STEP_SUMMARY-ready report from a real docs/render.jl/docs/make.jl build.\n\n\n\n\n\n","title":"DocumenterSlate.collect_build_statuses"},{"category":"function","location":"api/#DocumenterSlate.write_github_step_summary","page":"API Reference","text":"write_github_step_summary(io::IO, statuses) -> Nothing\n\nWrites a Markdown table (notebook slug, status, elapsed seconds) to io, suitable for $GITHUB_STEP_SUMMARY (REQ-CI-04). statuses is any iterable of objects exposing .slug, .status, and .elapsed_s — typically collect_build_statuses's return value, but deliberately duck-typed rather than requiring that exact type, so a caller assembling its own report (e.g. merging two builds) doesn't need to.\n\n\n\n\n\n","title":"DocumenterSlate.write_github_step_summary"},{"category":"section","location":"distribution/#Download-and-inspect-notebooks","page":"Distribution","text":"Every generated notebook page begins with a distribution panel. It links to the original .jl source, a reproducible .tar.gz archive, the resolved Julia environment, checksums, provenance, and local instructions. The source bytes are copied unchanged.\n\nThe prominent Download notebook action downloads the archive only. Inspect locally is a collapsed disclosure containing verification and inactive-preview commands; expanding it performs no network request and executes nothing on the reader's machine.\n\nBelow the actions, every notebook page prints the same four-step chain — download, verify, read, run — with that notebook's own filenames substituted in. The downloaded bundle's README.md repeats the identical chain, so a reader who kept only the archive still has the whole story. The steps are rendered from a single definition in DocumenterSlate, which is why the page and the bundle can never drift apart.","title":"Download and inspect notebooks"},{"category":"section","location":"distribution/#The-workflow,-end-to-end","page":"Distribution","text":"1. Download. fetch_slate_bundle is one call that downloads the archive over https, verifies every artifact against SHA256SUMS, cross-checks PROVENANCE.toml, and extracts the result. It evaluates no cell, instantiates no environment, and runs no package build script. It raises rather than returning an unverified bundle, so holding a SlateBundle means the bytes are the ones the publisher hashed:\n\nusing DocumenterSlate\n\nbundle = fetch_slate_bundle(\"https://example.org/docs/downloads/demo/demo.tar.gz\")\n\nSet SlateOutputOptions's canonical to your published site's base URL and each page prints that first line with the notebook's real absolute URL, ready to copy. Without it the page falls back to the archive's site-relative path and tells the reader to copy the link from the Download notebook button.\n\n2. Verify. Reaching step 2 means integrity is settled; origin is not. Checksums cannot tell you who built the bundle:\n\nbundle.provenance[\"repository\"]\nbundle.provenance[\"git_sha\"]\nbundle.provenance[\"ci_produced\"]\n\nCompare these against the repository you actually trust, and treat ci_produced = false as a locally built bundle. Provenance is build metadata, not a signature — anyone can serve a well-formed bundle. Displaying the returned bundle prints exactly these remaining steps.\n\n3. Read. bundle.source is plain Julia, and reading it is the only step that tells you what the code would actually do. KaimonSlate can serve a static view that starts no worker and evaluates no cell:\n\nusing KaimonSlate\nKaimonSlate.serve_notebook(bundle.source; inactive = true)\n\n4. Run deliberately. Only after steps 2 and 3, by one of the three routes in Run deliberately, ordered by host exposure. fetch_slate_bundle stops here on purpose: it will not launch the notebook for you, and no step of the chain is ever offered as a single chained command.\n\nnote: There is deliberately no one-click launch\nDocumenterSlate never offers a \"download and run this notebook from a URL\" action, and this is a design decision rather than a missing feature. A KaimonSlate notebook is arbitrary Julia code: instantiating its environment can run package build scripts, and opening it in live mode can execute anything. The documentation therefore never emits a custom URL scheme, never contacts a local server, and never suggests piping a remote download into Julia.Fetching a notebook over the network is supported — see Download from a URL — but only as an explicit verify-then-run sequence you carry out yourself. fetch_slate_bundle covers the fetch-and-verify half in one call precisely so the remaining half stays a separate, deliberate act.","title":"The workflow, end to end"},{"category":"section","location":"distribution/#Verify-before-opening","page":"Distribution","text":"Download the notebook directory or archive, then verify the files before using them:\n\nsha256sum -c SHA256SUMS\n\nPROVENANCE.toml records the repository, source commit, CI run identifier, build timestamp, Julia and package versions, source digest, and manifest digest. A missing CI run identifier is recorded honestly as a local build; this metadata is not a cryptographic attestation.\n\nDocumenterSlate can verify either the downloaded directory or its archive without evaluating notebook cells or instantiating the environment:\n\nusing DocumenterSlate\n\nresult = verify_slate_bundle(\"notebook.tar.gz\")\nresult.artifacts\nresult.provenance\n\nVerification rejects missing, extra, modified, duplicated, or dangerously named files. For an archive, links and nested paths are rejected before extraction. The source and optional manifest digests are also checked against PROVENANCE.toml. An integrity failure raises ArgumentError.","title":"Verify before opening"},{"category":"section","location":"distribution/#Download-from-a-URL","page":"Distribution","text":"Fetching a bundle over the network is fine; treating the fetch itself as a launch is not. Split it into two steps that you control, and never chain them into a single command.\n\nFirst, download the archive to a local path and check what you received. fetch_slate_bundle does both, and refuses anything but https:\n\nusing DocumenterSlate\n\nbundle = fetch_slate_bundle(\"https://example.org/notebooks/downloads/demo/demo.tar.gz\")\nbundle.verification.artifacts\nbundle.provenance\n\nThe two halves are also available separately, when you want to control the download yourself:\n\nusing Downloads, DocumenterSlate\n\narchive = Downloads.download(\"https://example.org/notebooks/downloads/demo/demo.tar.gz\",\n                             joinpath(mktempdir(), \"demo.tar.gz\"))\nresult = verify_slate_bundle(archive)\nresult.artifacts\nresult.provenance\n\nverify_slate_bundle reads the archive's entries and rejects links, nested paths, and non-regular files before extracting anything, then checks every artifact against SHA256SUMS and cross-checks the notebook source and optional manifest against PROVENANCE.toml. It never evaluates notebook cells and never instantiates the environment, so it is safe to run on a bundle you have not yet reviewed. An integrity or structural failure raises ArgumentError.\n\nOnly after verification succeeds, and after reading the .jl source, extract the archive and use one of the routes in Run deliberately — the container route first.\n\nresult.provenance tells you where the bundle claims to come from, not that the claim is true: it is build metadata, not a signature. Compare repository and git_sha against the repository you actually trust, and treat ci_produced = false as a locally built bundle. Until forge attestations are authenticated, a URL is not evidence of origin — anyone can serve a well-formed bundle.","title":"Download from a URL"},{"category":"section","location":"distribution/#Inspect-without-evaluating-cells","page":"Distribution","text":"A KaimonSlate notebook is plain Julia source, so reading the .jl file is the safest first step. KaimonSlate can also serve a static inspection view without starting a worker or evaluating cells:\n\nusing KaimonSlate\nKaimonSlate.serve_notebook(\"notebook.jl\"; inactive = true)\n\nThis deliberately is not a one-click launch mechanism. The documentation never emits a custom URL scheme, contacts a local server, or suggests piping a remote download into Julia.","title":"Inspect without evaluating cells"},{"category":"section","location":"distribution/#Run-deliberately","page":"Distribution","text":"The downloaded README.md presents three routes in increasing order of host exposure:\n\na Podman container using the recorded Julia minor version;\nthe published Project.toml and Manifest.toml on the host;\nthe native slate application.\n\nInstantiating a Julia environment can run package build scripts. Opening a notebook in live mode can execute arbitrary code. A container reduces access to the host, but it does not make untrusted code harmless; review the mount and network permissions as well.","title":"Run deliberately"},{"category":"section","location":"distribution/#Reproducibility-and-current-scope","page":"Distribution","text":"Archives use sorted entries, a zero timestamp, normalized ownership and normalized file modes. They are written in-process with Tar.jl, so every entry is a flat regular file and the build does not depend on a system tar. SHA256SUMS covers every artifact except itself. HTML/PDF exports, self-contained KaimonSlate bundles, and forge attestations remain later roadmap work. Until attestation authentication is implemented, verify_slate_bundle rejects an archive containing an attestation rather than presenting unverified metadata as trusted.","title":"Reproducibility and current scope"},{"category":"section","location":"notebooks/hidden_code_demo/","page":"Hiding a cell's source","text":"<div class=\"slate-actions\">\n  <a class=\"button is-link slate-download-button\" href=\"downloads/hidden_code_demo/hidden_code_demo.tar.gz\" download>Download notebook</a>\n  <details class=\"slate-inspect\">\n    <summary>Inspect locally</summary>\n    <p><strong>Do not run the notebook before reviewing it.</strong> If you already hold the files, these two calls verify the archive and open KaimonSlate's inactive view, which starts no worker and evaluates no cell. The full download-to-run workflow is below.</p>\n    <pre><code>using DocumenterSlate\nverify_slate_bundle(\"hidden_code_demo.tar.gz\")</code></pre>\n    <pre><code>using KaimonSlate\nKaimonSlate.serve_notebook(\"hidden_code_demo.jl\"; inactive = true)</code></pre>\n  </details>\n</div>\n\nwarning: Download, verify, read, then run — in that order\nThis notebook is arbitrary code. Instantiating its environment may run package build scripts, and live opening may execute cells. There is deliberately no one-click launch: each step below is an act you perform after reading the previous one.1. DownloadOne call downloads the archive, checks every artifact against SHA256SUMS, cross-checks PROVENANCE.toml, and extracts the result. It evaluates no cell and instantiates no environment.Copy the Download notebook link above — it is downloads/hidden_code_demo/hidden_code_demo.tar.gz on this site — and fetch it:using DocumenterSlate\nbundle = fetch_slate_bundle(\"https://<this site>/downloads/hidden_code_demo/hidden_code_demo.tar.gz\")2. VerifyStep 1 raises rather than returning an unverified bundle, so reaching this step means the bytes are intact. If you took the download directory instead, check it with the checksums it carries:sha256sum -c SHA256SUMSWhat checksums cannot establish, either way, is origin:bundle.provenance[\"repository\"]\nbundle.provenance[\"git_sha\"]\nbundle.provenance[\"ci_produced\"]Compare repository and git_sha against the repository you actually trust, and treat ci_produced = false as a locally built bundle. Provenance is build metadata, not a signature — anyone can serve a well-formed bundle.3. ReadA KaimonSlate notebook is plain Julia source, and reading hidden_code_demo.jl is the only step that tells you what the code would actually do. KaimonSlate can also serve a static view that starts no worker and evaluates no cell:using KaimonSlate\nKaimonSlate.serve_notebook(bundle.source; inactive = true)4. Run deliberatelyOnly after steps 2 and 3. Instantiating the environment may run package build scripts, and opening the notebook in live mode may execute anything. The three routes below are ordered by how much of your host they expose.Isolated container (recommended) — keeps package installation and notebook inspection off the host. The image opens the notebook in inactive mode; it evaluates no cell:podman build -t hidden_code_demo-inspect . && podman run --rm -it -p 8765:8765 hidden_code_demo-inspectAlternatively, open devcontainer.json in a compatible editor.Pinned host environment — uses the published project and manifest, but runs package build scripts and notebook code with your host account's permissions:julia --project=. -e 'using Pkg; Pkg.instantiate()'\njulia --project=. -e 'using KaimonSlate; KaimonSlate.serve_notebook(\"hidden_code_demo.jl\")'Native application — installing and opening the application executes code with your host account's permissions:julia -e 'using Pkg; Pkg.Registry.add(Pkg.Registry.RegistrySpec(url=\"https://github.com/kahliburke/SlateRegistry\")); Pkg.Apps.add(\"KaimonSlate\")'\nslate --own hidden_code_demo.jlA container reduces access to the host; it does not make untrusted code harmless. Review the mount and network permissions too.Download source · Checksums · Provenance · Local instructionsArtifact SHA-256\nDockerfile 1214607cf95b8322d39611858f4e19572ac3c7a8a07b9d8fc723de9de02ae354\nManifest.toml c56060cd8c6a3327243c2f499e3c0114587736063c6889047e9470f77b6161dc\nPROVENANCE.toml 861c9d3805a6a2a79489a5b32bd551ca607a62cadfadcf0fd5c09b4ea4232645\nProject.toml f611f3118041332ea446aa24d11ad03397fcd156b61a97ccd4d0313008befb04\nREADME.md c47bbb780b7ef690874752c8113e6ddb042d4829060e1615cbf64f993ec24798\ndevcontainer.json 64a9fc063666df831bf6f932766e9980c81d4c74d17e0d0ec867287542789ee7\nhidden_code_demo.jl 9f2a885759e883277fef7053a9e022caeb2ce842ea313800222b46757287ad23\nhidden_code_demo.tar.gz d8ab382f707e125d8ef0a133486370253de62b2c468e39114aef4ed8df1eb507","title":"Hiding a cell's source"},{"category":"section","location":"notebooks/hidden_code_demo/#hidden_code_demo-hiding-a-cell-s-source","page":"Hiding a cell's source","text":"A code cell tagged hidecode keeps its output on the page while hiding its source — useful when the result matters to the reader but the mechanics of producing it would be noise (REQ-REN-05). This always wins over the site-wide show_code default; it's the cell author's own explicit choice, not a global toggle.\n\nThe cell below is an ordinary cell — its source is visible, as usual.\n\nmessage = \"you can see this cell's source\"\n\nyou can see this cell's source\n\nThe next cell is tagged hidecode. Its source (a short computation building the same kind of value) does not appear on the page — only its output does.\n\n385","title":"Hiding a cell's source"},{"category":"section","location":"notebooks/downloads/hidden_code_demo/README/#hidden*code*demo-—-local-inspection","page":"hiddencodedemo — local inspection","text":"This directory contains untrusted notebook code. Verify the files before opening them, then read the source before running it. The four steps below are the whole story, in order; the site this bundle came from shows the same chain.","title":"hiddencodedemo — local inspection"},{"category":"section","location":"notebooks/downloads/hidden_code_demo/README/#1.-Download","page":"hiddencodedemo — local inspection","text":"You already have this directory. If you also kept the archive it came from, verify that before trusting anything extracted from it:\n\nusing DocumenterSlate\nverify_slate_bundle(\"hidden_code_demo.tar.gz\")","title":"1. Download"},{"category":"section","location":"notebooks/downloads/hidden_code_demo/README/#2.-Verify","page":"hiddencodedemo — local inspection","text":"Check the files against the checksums the bundle carries:\n\nsha256sum -c SHA256SUMS\n\nor, equivalently, from Julia:\n\nusing DocumenterSlate\nverify_slate_bundle(\".\")\n\nThen read PROVENANCE.toml. Checksums prove the bytes are intact; they say nothing about origin. Compare repository and git_sha against the repository you actually trust, and treat ci_produced = false as a locally built bundle. Provenance is build metadata, not a signature — anyone can serve a well-formed bundle.","title":"2. Verify"},{"category":"section","location":"notebooks/downloads/hidden_code_demo/README/#3.-Read","page":"hiddencodedemo — local inspection","text":"A KaimonSlate notebook is plain Julia source, and reading hidden_code_demo.jl is the only step that tells you what the code would actually do. KaimonSlate can also serve a static view that starts no worker and evaluates no cell:\n\nusing KaimonSlate\nKaimonSlate.serve_notebook(\"hidden_code_demo.jl\"; inactive = true)","title":"3. Read"},{"category":"section","location":"notebooks/downloads/hidden_code_demo/README/#4.-Run-deliberately","page":"hiddencodedemo — local inspection","text":"Only after steps 2 and 3. Instantiating the environment may run package build scripts, and opening the notebook in live mode may execute anything. The three routes below are ordered by how much of your host they expose.\n\nIsolated container (recommended) — keeps package installation and notebook inspection off the host. The image opens the notebook in inactive mode; it evaluates no cell:\n\npodman build -t hidden_code_demo-inspect . && podman run --rm -it -p 8765:8765 hidden_code_demo-inspect\n\nAlternatively, open devcontainer.json in a compatible editor.\n\nPinned host environment — uses the published project and manifest, but runs package build scripts and notebook code with your host account's permissions:\n\njulia --project=. -e 'using Pkg; Pkg.instantiate()'\njulia --project=. -e 'using KaimonSlate; KaimonSlate.serve_notebook(\"hidden_code_demo.jl\")'\n\nNative application — installing and opening the application executes code with your host account's permissions:\n\njulia -e 'using Pkg; Pkg.Registry.add(Pkg.Registry.RegistrySpec(url=\"https://github.com/kahliburke/SlateRegistry\")); Pkg.Apps.add(\"KaimonSlate\")'\nslate --own hidden_code_demo.jl\n\nA container reduces access to the host; it does not make untrusted code harmless. Review the mount and network permissions too.","title":"4. Run deliberately"},{"category":"section","location":"notebooks/hello/","page":"Hello, DocumenterSlate","text":"<div class=\"slate-actions\">\n  <a class=\"button is-link slate-download-button\" href=\"downloads/hello/hello.tar.gz\" download>Download notebook</a>\n  <details class=\"slate-inspect\">\n    <summary>Inspect locally</summary>\n    <p><strong>Do not run the notebook before reviewing it.</strong> If you already hold the files, these two calls verify the archive and open KaimonSlate's inactive view, which starts no worker and evaluates no cell. The full download-to-run workflow is below.</p>\n    <pre><code>using DocumenterSlate\nverify_slate_bundle(\"hello.tar.gz\")</code></pre>\n    <pre><code>using KaimonSlate\nKaimonSlate.serve_notebook(\"hello.jl\"; inactive = true)</code></pre>\n  </details>\n</div>\n\nwarning: Download, verify, read, then run — in that order\nThis notebook is arbitrary code. Instantiating its environment may run package build scripts, and live opening may execute cells. There is deliberately no one-click launch: each step below is an act you perform after reading the previous one.1. DownloadOne call downloads the archive, checks every artifact against SHA256SUMS, cross-checks PROVENANCE.toml, and extracts the result. It evaluates no cell and instantiates no environment.Copy the Download notebook link above — it is downloads/hello/hello.tar.gz on this site — and fetch it:using DocumenterSlate\nbundle = fetch_slate_bundle(\"https://<this site>/downloads/hello/hello.tar.gz\")2. VerifyStep 1 raises rather than returning an unverified bundle, so reaching this step means the bytes are intact. If you took the download directory instead, check it with the checksums it carries:sha256sum -c SHA256SUMSWhat checksums cannot establish, either way, is origin:bundle.provenance[\"repository\"]\nbundle.provenance[\"git_sha\"]\nbundle.provenance[\"ci_produced\"]Compare repository and git_sha against the repository you actually trust, and treat ci_produced = false as a locally built bundle. Provenance is build metadata, not a signature — anyone can serve a well-formed bundle.3. ReadA KaimonSlate notebook is plain Julia source, and reading hello.jl is the only step that tells you what the code would actually do. KaimonSlate can also serve a static view that starts no worker and evaluates no cell:using KaimonSlate\nKaimonSlate.serve_notebook(bundle.source; inactive = true)4. Run deliberatelyOnly after steps 2 and 3. Instantiating the environment may run package build scripts, and opening the notebook in live mode may execute anything. The three routes below are ordered by how much of your host they expose.Isolated container (recommended) — keeps package installation and notebook inspection off the host. The image opens the notebook in inactive mode; it evaluates no cell:podman build -t hello-inspect . && podman run --rm -it -p 8765:8765 hello-inspectAlternatively, open devcontainer.json in a compatible editor.Pinned host environment — uses the published project and manifest, but runs package build scripts and notebook code with your host account's permissions:julia --project=. -e 'using Pkg; Pkg.instantiate()'\njulia --project=. -e 'using KaimonSlate; KaimonSlate.serve_notebook(\"hello.jl\")'Native application — installing and opening the application executes code with your host account's permissions:julia -e 'using Pkg; Pkg.Registry.add(Pkg.Registry.RegistrySpec(url=\"https://github.com/kahliburke/SlateRegistry\")); Pkg.Apps.add(\"KaimonSlate\")'\nslate --own hello.jlA container reduces access to the host; it does not make untrusted code harmless. Review the mount and network permissions too.Download source · Checksums · Provenance · Local instructionsArtifact SHA-256\nDockerfile b9b5a65cc1ebe0fdc21625b77480f709e39dd2cd908218f5d4681b164c3cff1b\nManifest.toml c56060cd8c6a3327243c2f499e3c0114587736063c6889047e9470f77b6161dc\nPROVENANCE.toml 28879da31e106d2ce1cb3771a92efb0e9c0571e9759ee39fef5afbfb4a860fb5\nProject.toml f611f3118041332ea446aa24d11ad03397fcd156b61a97ccd4d0313008befb04\nREADME.md fad8bba783ac355a8225c1d83051a5061f0c3aa9b3feade080a9ae9656c784d5\ndevcontainer.json 8776646c7150dcd77690c6173db06c928226aece219289217ce264ba8622c525\nhello.jl 3efd9ee02987f58d97d230b8355192eb521d6accc4dbf4b18a658ec674034601\nhello.tar.gz ffc01ed1bb0313b69df9852ae20384840bfb8396bee498ed77424959f0030bff","title":"Hello, DocumenterSlate"},{"category":"section","location":"notebooks/hello/#hello-hello-documenterslate","page":"Hello, DocumenterSlate","text":"This page is not a mockup — it is a real KaimonSlate.jl notebook (docs/notebooks/hello.jl in this repository), executed headlessly by build_slates and rendered into this Documenter page, exactly the way a package maintainer would use DocumenterSlate.jl for their own tutorials.","title":"Hello, DocumenterSlate"},{"category":"section","location":"notebooks/hello/#hello-a-small-computation","page":"Hello, DocumenterSlate","text":"The cell below runs in a fresh Julia module, with its source shown above its output — show_code defaults to true (REQ-REN-05).\n\nn = 12\nfactorial(n)\n\n479001600\n\nStandard output is captured too, fenced as plain text rather than a Documenter doctest (REQ-REN-10) so a future change to this notebook's printed output never breaks the package's own test suite the way a jldoctest block would.\n\nprintln(\"Hello from a real notebook, executed in CI.\")\n\"nice to meet you\"\n\nHello from a real notebook, executed in CI.\n\nnice to meet you","title":"A small computation"},{"category":"section","location":"security/#Security-posture","page":"Security","text":"This page states plainly what the reference two-job CI workflow (.github/workflows/docs.yml) guarantees, and — just as important — what it does not, yet. Read this before adopting the same build model for your own package. See spec.md (repository root, not shipped in this site) §12 for the full threat model (T1–T6) this page is a summary of.","title":"Security posture"},{"category":"section","location":"security/#What-the-two-job-split-guarantees-(REQ-SEC-03)","page":"Security","text":"render-notebooks executes untrusted notebook code and holds no secrets — no DOCUMENTER_KEY, no write-scoped GITHUB_TOKEN. A malicious or compromised notebook dependency running in this job has nothing valuable to exfiltrate through CI's own credentials.\nbuild-and-deploy holds DOCUMENTER_KEY and runs build_slates(...; execution = :never) — this never executes a single cell. A cache miss raises an error rather than falling back to execution (REQ-EXE-10). This is enforced in code (build_slates's docstring, _build_one_notebook's execution == :never branch), not only by workflow convention.","title":"What the two-job split guarantees (REQ-SEC-03)"},{"category":"section","location":"security/#What-is-*not*-guaranteed-yet:-cache-entry-provenance-(T6)","page":"Security","text":"execution = :never's cache lookup validates a fingerprint computed purely from inputs (notebook bytes, resolved environment, tool versions, render options) and SHA-256 hashes of the rendered Markdown, assets, and resolved environment. These hashes detect accidental or external corruption, but they do not cryptographically bind a cache entry to who produced it. Concretely: a notebook running in render-notebooks has ordinary filesystem access to docs/slate_cache in that same job. A malicious notebook merged into notebooks//docs/notebooks/ could in principle write a forged <slug>.md + matching <slug>.cache.toml pair directly, bypassing cell_to_markdown's escaping entirely for that page.\n\nThis does not let a malicious notebook steal secrets or execute code in the privileged job — execution = :never still never executes anything. Per-notebook PROVENANCE.toml files now record the source commit and CI run as distribution metadata, but they are generated after the cache lookup and are not a cryptographic producer binding. Closing this cache-specific gap requires a forge build attestation (REQ-TRUST-03) or an equivalent authenticated handoff. Mitigate the same way you'd mitigate any other risk from a PR touching notebooks/: review those changes at the same rigor as source code (spec.md §12's own T6 mitigation note makes the same recommendation).","title":"What is not guaranteed yet: cache-entry provenance (T6)"},{"category":"section","location":"security/#Agent-isolation-(REQ-SEC-01)","page":"Security","text":"TextualReplayExporter — the only execution backend implemented so far — never starts KaimonSlate's interactive hub or reaches its AI agent. This was confirmed by reading KaimonSlate.jl's source directly (not assumed): the exporter's only calls into KaimonSlate (standalone!, parse_report) never touch _hub() or the KAIMON_EXTENSION-gated agent registration. A regression test (test/test_execute.jl) checks this at runtime, not only in documentation.\n\nThis is specific to the current backend. A future HubExporter (driving KaimonSlate's interactive hub directly, not yet implemented) would reopen this question — the hub does expose the agent's tool-calling surface, and no upstream disable mechanism has been confirmed to exist. Do not assume this guarantee carries over once a hub-based exporter ships.","title":"Agent isolation (REQ-SEC-01)"},{"category":"section","location":"security/#Process,-project,-and-environment-isolation-(REQ-EXE-02,-REQ-SEC-02)","page":"Security","text":"build_slates renders every cache miss in a dedicated Julia child process. For a notebook with an adjacent Project.toml, that project is activated before cells run. A timeout terminates and waits for the operating-system process, including a worker stuck in non-yielding code. The adjacent project must already be instantiated; documentation builds do not modify it or fetch dependencies implicitly.\n\nThe child does not inherit the parent's environment. Only Julia's internal depot/load-path/thread settings and SlateBuildOptions.worker_environment are passed. The latter is an explicit Dict{String,String}; use it only for reviewed, non-secret values that the notebook genuinely needs. Its contents affect the cache fingerprint.\n\nFor a notebook carrying only a Slate.env footer, DocumenterSlate creates a temporary project from the declared name/UUID/version entries in another isolated process, resolves and instantiates it, and publishes the resulting Project.toml and Manifest.toml. The cache artifact carries that resolved environment into the non-executing deployment job, which never resolves packages.\n\nThe lower-level execute_notebook API remains an in-process primitive and can see the caller's process-global ENV. Use build_slates for isolated documentation builds.","title":"Process, project, and environment isolation (REQ-EXE-02, REQ-SEC-02)"},{"category":"section","location":"security/#Network-egress-(REQ-SEC-04)-—-monitored,-not-yet-blocked","page":"Security","text":"Both jobs run behind step-security/harden-runner (pinned to a commit SHA, not a floating tag) in egress-policy: audit mode — every outbound connection is logged and checked against StepSecurity's always-on malicious-domain list, but nothing legitimate is blocked yet. This is deliberately the tool's own recommended starting point, not a shortcut: an earlier attempt at egress-policy: block with a guessed allowed-endpoints list was caught by review before merging — the guess missed the actual redirect chain Julia's package client follows through pkg.julialang.org (a region-specific mirror, then a separate storage backend), which would have made every render-notebooks run fail outright once merged. Shipping a broken block-list is worse than the gap it closes.\n\nUpgrading to block is the natural next step, once a real workflow run's harden-runner insights confirm the exact endpoint set actually used — not before. This is a CI-infra control, not a SlateBuildOptions option — there is no network = :deny field on SlateBuildOptions. The separate allow_remote = false default rejects notebooks containing region=<name> cell tags unless the caller explicitly opts in (REQ-EXE-09); it controls declared KaimonSlate remote placement, not arbitrary network access by notebook code.","title":"Network egress (REQ-SEC-04) — monitored, not yet blocked"},{"category":"section","location":"security/#Remote-region-opt-in-(REQ-EXE-09)","page":"Security","text":"Before consulting the cache or executing cells, build_slates parses every notebook with KaimonSlate's parser and rejects any cell tagged region=<name> while SlateBuildOptions.allow_remote == false (the default). Set allow_remote = true only after reviewing the notebook's declared placement. The current TextualReplayExporter still replays cells locally and never starts remote workers; the opt-in preserves the authorization boundary for future exporters that can honor region placement.","title":"Remote-region opt-in (REQ-EXE-09)"},{"category":"section","location":"security/#Content-escaping-and-path-traversal-(REQ-SEC-05/06)","page":"Security","text":"discover_notebooks structurally rejects any notebook path that resolves outside its declared source directory, including via a symlink (REQ-SEC-06).\ncell_to_markdown fences all derived/executed content (stdout, values, error messages) so it renders as literal text regardless of what it contains — adversarially tested against HTML-special payloads, not merely assumed safe (REQ-SEC-05). A notebook's own markdown prose is intentionally passed through unmodified, the same as any Documenter .md source file — that is authored content, not derived output, and carries the same trust level as the rest of your documentation source.","title":"Content escaping and path traversal (REQ-SEC-05/06)"},{"category":"section","location":"security/#Distribution-and-download","page":"Security","text":"The sections above cover the build side — what happens inside CI, and what the cache is and is not trusted for. The download side has its own posture, documented in full under Download and inspect notebooks: DocumenterSlate deliberately offers no one-click \"run this notebook from a URL\" action. It emits no custom URL scheme, contacts no local server, and never suggests piping a remote download into Julia.\n\nFetching over the network is supported as an explicit two-step sequence — download to a local path, then verify_slate_bundle, which checks an exhaustive SHA256SUMS and rejects links, nested paths, and non-regular archive entries before extracting, without evaluating a single cell. What that buys you is integrity, not origin: PROVENANCE.toml is build metadata, not a signature, and an attestation-bearing bundle is rejected outright rather than presented as trusted (REQ-TRUST-03 remains open).","title":"Distribution and download"},{"category":"section","location":"security/#What-none-of-this-proves","page":"Security","text":"An attestation or a clean cache lookup proves an artifact's origin — that it came from this workflow, at this commit. It proves nothing about whether the notebook itself is well-behaved. A malicious notebook authored and merged by a maintainer remains malicious however cleanly it was built (spec.md §11.5's own point, restated here for the CI/build-cache context rather than the download/distribution context it was originally written for).","title":"What none of this proves"},{"category":"section","location":"notebooks/downloads/hello/README/#hello-—-local-inspection","page":"hello — local inspection","text":"This directory contains untrusted notebook code. Verify the files before opening them, then read the source before running it. The four steps below are the whole story, in order; the site this bundle came from shows the same chain.","title":"hello — local inspection"},{"category":"section","location":"notebooks/downloads/hello/README/#1.-Download","page":"hello — local inspection","text":"You already have this directory. If you also kept the archive it came from, verify that before trusting anything extracted from it:\n\nusing DocumenterSlate\nverify_slate_bundle(\"hello.tar.gz\")","title":"1. Download"},{"category":"section","location":"notebooks/downloads/hello/README/#2.-Verify","page":"hello — local inspection","text":"Check the files against the checksums the bundle carries:\n\nsha256sum -c SHA256SUMS\n\nor, equivalently, from Julia:\n\nusing DocumenterSlate\nverify_slate_bundle(\".\")\n\nThen read PROVENANCE.toml. Checksums prove the bytes are intact; they say nothing about origin. Compare repository and git_sha against the repository you actually trust, and treat ci_produced = false as a locally built bundle. Provenance is build metadata, not a signature — anyone can serve a well-formed bundle.","title":"2. Verify"},{"category":"section","location":"notebooks/downloads/hello/README/#3.-Read","page":"hello — local inspection","text":"A KaimonSlate notebook is plain Julia source, and reading hello.jl is the only step that tells you what the code would actually do. KaimonSlate can also serve a static view that starts no worker and evaluates no cell:\n\nusing KaimonSlate\nKaimonSlate.serve_notebook(\"hello.jl\"; inactive = true)","title":"3. Read"},{"category":"section","location":"notebooks/downloads/hello/README/#4.-Run-deliberately","page":"hello — local inspection","text":"Only after steps 2 and 3. Instantiating the environment may run package build scripts, and opening the notebook in live mode may execute anything. The three routes below are ordered by how much of your host they expose.\n\nIsolated container (recommended) — keeps package installation and notebook inspection off the host. The image opens the notebook in inactive mode; it evaluates no cell:\n\npodman build -t hello-inspect . && podman run --rm -it -p 8765:8765 hello-inspect\n\nAlternatively, open devcontainer.json in a compatible editor.\n\nPinned host environment — uses the published project and manifest, but runs package build scripts and notebook code with your host account's permissions:\n\njulia --project=. -e 'using Pkg; Pkg.instantiate()'\njulia --project=. -e 'using KaimonSlate; KaimonSlate.serve_notebook(\"hello.jl\")'\n\nNative application — installing and opening the application executes code with your host account's permissions:\n\njulia -e 'using Pkg; Pkg.Registry.add(Pkg.Registry.RegistrySpec(url=\"https://github.com/kahliburke/SlateRegistry\")); Pkg.Apps.add(\"KaimonSlate\")'\nslate --own hello.jl\n\nA container reduces access to the host; it does not make untrusted code harmless. Review the mount and network permissions too.","title":"4. Run deliberately"},{"category":"section","location":"","page":"Home","text":"<div id=\"landing\" class=\"landing\">\n<header class=\"landing-hero\">\n  <div class=\"landing-hero__text\">\n    <p class=\"landing-name\">DocumenterSlate.jl</p>\n    <h1 class=\"landing-title\">Reactive notebooks, native documentation</h1>\n    <p class=\"landing-tagline\">Build KaimonSlate notebooks in isolated CI workers, then publish verified, reproducible output as first-class Documenter pages.</p>\n    <div class=\"landing-actions\">\n<a class=\"landing-btn landing-btn--brand\" href=\"#Quick-start\">Get started</a>\n      <a class=\"landing-btn landing-btn--alt\" href=\"notebooks/hello/\">See a real notebook</a>\n    </div>\n  </div>\n  <div class=\"landing-hero__image\">\n    <div class=\"landing-hero__image-container\">\n      <img src=\"assets/logo.svg\" alt=\"A notebook rendered as a Documenter page\" width=\"320\" height=\"320\">\n    </div>\n  </div>\n</header>\n<section class=\"landing-features\" aria-label=\"Features\">\n  <a class=\"landing-feature\" href=\"notebooks/hello/\">\n<div class=\"landing-feature__icon\">📖</div>\n<h2 class=\"landing-feature__title\">Native Documenter pages</h2>\n<p class=\"landing-feature__details\">Searchable, linkable notebook output lives beside the rest of your package documentation—not in an iframe or a separate app.</p>\n  </a>\n  <a class=\"landing-feature\" href=\"security/\">\n<div class=\"landing-feature__icon\">🛡️</div>\n<h2 class=\"landing-feature__title\">Split, safer CI</h2>\n<p class=\"landing-feature__details\">Execute notebooks without deployment secrets, then build and deploy from the resulting cache in a separate job.</p>\n  </a>\n  <div class=\"landing-feature\">\n<div class=\"landing-feature__icon\">📦</div>\n<h2 class=\"landing-feature__title\">Reproducible environments</h2>\n<p class=\"landing-feature__details\">Every cache miss runs in its own Julia process with the notebook's adjacent project activated and an explicit environment allowlist.</p>\n  </div>\n  <a class=\"landing-feature\" href=\"distribution/\">\n<div class=\"landing-feature__icon\">✓</div>\n<h2 class=\"landing-feature__title\">Verifiable downloads</h2>\n<p class=\"landing-feature__details\">Publish source, environment, checksums, and provenance so readers can verify a bundle before inspecting or running it.</p>\n  </a>\n  <a class=\"landing-feature\" href=\"#Embed-a-notebook\">\n<div class=\"landing-feature__icon\">🧩</div>\n<h2 class=\"landing-feature__title\">Pages or embeds</h2>\n<p class=\"landing-feature__details\">Add every notebook to navigation automatically, or place one inside an authored guide with the @slate directive.</p>\n  </a>\n  <div class=\"landing-feature\">\n<div class=\"landing-feature__icon\">⚡</div>\n<h2 class=\"landing-feature__title\">Cache-aware builds</h2>\n<p class=\"landing-feature__details\">Rebuild only when notebook inputs change; the deploy job is forbidden from silently executing a cache miss.</p>\n  </div>\n</section>\n</div>","title":"Home"},{"category":"section","location":"#Quick-start","page":"Home","text":"DocumenterSlate is not yet registered. Add it directly from GitHub to the environment that builds your documentation:\n\nimport Pkg\nPkg.add(url = \"https://github.com/s-celles/DocumenterSlate.jl\")\n\nRender the notebooks before calling makedocs:\n\nusing Documenter, DocumenterSlate\n\nresult = build_slates(\n    SlateBuildOptions(;\n        source = joinpath(@__DIR__, \"notebooks\"),\n        output = joinpath(@__DIR__, \"src\", \"notebooks\"),\n        worker_environment = Dict(\"LANG\" => \"C.UTF-8\"),\n    ),\n)\n\nmakedocs(;\n    sitename = \"MyPkg.jl\",\n    plugins = [SlatePlugin(result)],\n    pages = [\n        \"Home\" => \"index.md\",\n        \"Notebooks\" => result.pages,\n    ],\n)\n\nEach cache miss runs in a separate Julia process with the notebook's adjacent project activated. The parent environment is denied by default; worker_environment is the explicit allowlist.","title":"Quick start"},{"category":"section","location":"#How-the-build-stays-predictable","page":"Home","text":"build_slates discovers notebooks and hashes every relevant input.\nA render job executes cache misses without documentation deployment credentials.\nA separate deploy job consumes that exact cache with execution = :never and publishes the generated Markdown, assets, and downloadable bundles through Documenter.\n\nThis documentation site uses that two-job workflow itself. The Notebooks section is generated from the real KaimonSlate sources in this repository, not from fixtures or hand-written output.\n\nwarning: Treat notebooks as code\nNotebook cells are arbitrary Julia code. Read the security model before using the reference workflow for your own package, and verify downloaded bundles before opening them in a live runtime.","title":"How the build stays predictable"},{"category":"section","location":"#Embed-a-notebook","page":"Home","text":"Install SlatePlugin(result) as above, then place a pre-rendered notebook inside an authored page:\n\n```@slate\nnotebook = \"analysis.jl\"\n```\n\nUse @slate-download with the same notebook = \"analysis.jl\" configuration when the page should contain only the download and inactive-inspection panel.","title":"Embed a notebook"},{"category":"section","location":"#Explore-further","page":"Home","text":"See a generated notebook to inspect the rendered result.\nUnderstand downloads and verification before sharing bundles.\nRead the security model before adopting the CI workflow.\nUse the API reference when you need the exact configuration surface.","title":"Explore further"}]
}
